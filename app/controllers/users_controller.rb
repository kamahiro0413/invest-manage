class UsersController < ApplicationController
  def show
    # @investment = Investment.new
    # @group = Group.find(params[:group_id])
    # @investments = @group.investments.includes(:user)
  end
end
